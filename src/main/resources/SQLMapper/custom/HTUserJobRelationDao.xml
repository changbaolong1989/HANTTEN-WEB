<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hd.manager.dao.HTUserJobRelationDao">
    <resultMap id="BaseResultMap" type="com.hd.common.entity.TbUserJobRelation">
        <id column="user_id" jdbcType="VARCHAR" property="userId"/>
        <id column="job_id" jdbcType="VARCHAR" property="jobId"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    <sql id="Base_Column_List">
    user_id, job_id
  </sql>
    <select id="selectIncumbentUser" parameterType="com.hd.common.entity.TbUserJobRelationExample"
            resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        t.user_id, t.job_id
        from tb_user_job_relation t
        LEFT JOIN tb_user t1 ON t.user_id = t1.user_id
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
        <![CDATA[ AND t1.is_incumbent <> '0' ]]>
        <![CDATA[ AND t1.is_del_flg = '0' ]]>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
        <if test="limitStart != null and limitStart>=0">
            limit #{limitStart} , #{limitEnd}
        </if>
    </select>
</mapper>